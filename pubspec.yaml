name: we_shop
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: ^3.6.0


dependencies:
  flutter:
    sdk: flutter

  firebase_auth: ^5.3.4 
  cupertino_icons: ^1.0.8
  firebase_core: ^3.9.0
  http: ^1.2.2
  html: ^0.15.0
  xml: ^6.1.0
  firebase_database: ^11.2.0
  firebase_cloud_firestore: ^5.4.4
  pretty_qr_code: ^3.3.0
  mobile_scanner: ^6.0.2


dev_dependencies:
  flutter_test:
    sdk: flutter

  
  flutter_lints: ^5.0.0


flutter:

  
  uses-material-design: true



  assets:
    - assets/background_images.jpg # Path to your image
    - assets/home.png
 # Path to your image

